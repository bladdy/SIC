@page "/my-album/{Code}"

<PageTitle>Álbum</PageTitle>

<div style="padding: 1rem; font-family: Arial, sans-serif;">

    <!-- COVER -->
    <div style="
        width: 100%;
        aspect-ratio: 3 / 1;
        max-height: 320px;
        margin-bottom: 1.5rem;
        border-radius: 12px;
        overflow: hidden;
        background-image: url('@Event?.CoverAlbumImageUrl');
        background-size: cover;
        background-repeat: no-repeat;
        background-position:
        @($"{Event?.CoverPositionX ?? 50}% {Event?.CoverPositionY ?? 50}%");
    ">
    </div>

    <!-- HEADER -->
    <div style="margin-bottom: 1.5rem;">

        <!-- Fila superior -->
        <div style="display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 1rem;">

            <!-- Nombre -->
            <h2 style="margin: 0; color: #3C6A79;">
                @Event?.Name
            </h2>

            <!-- Botones -->
            <div style="display: flex; gap: 10px; flex-wrap: wrap;">
                <button @onclick="() => DownloadAllImages(Code!)"
                        style="padding: 0.5rem 1rem; background-color: #3C6A79; color: white; border: none; border-radius: 6px; cursor: pointer;">
                    <i class="bi bi-download"></i> Descargar Imágenes
                </button>

                <button @onclick="() => GenerateQR(Code!)"
                        style="padding: 0.5rem 1rem; background-color: #3C6A79; color: white; border: none; border-radius: 6px; cursor: pointer;">
                    <i class="bi bi-qr-code"></i> Descargar QR
                </button>
            </div>
        </div>

        <!-- Fila inferior -->
        <h4 style="margin: 0.25rem 0 0; color: #3C6A79;">
            Galería (@EventImages?.Count)
        </h4>

    </div>

    <!-- CONTENIDO -->
    <!-- CONTENIDO -->
    @if (EventImages == null)
    {
        <p style="color: #6c757d;">Cargando archivos...</p>
    }
    else if (!EventImages.Any())
    {
        <div style="padding: 2rem; text-align: center; color: #6c757d; border: 1px dashed #dee2e6; border-radius: 6px;">
            <p>No hay archivos en este álbum aún.</p>
        </div>
    }
    else
    {
        <div style="
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
        gap: 1rem;
            ">

            @foreach (var image in EventImages)
            {
                <div style="
                overflow: hidden;
                border-radius: 10px;
                background: #f5f5f5;
                cursor: pointer;
                position: relative;
                        ">

                    @if (image.ImageType == "imagen")
                    {
                        <img src="@image.Url"
                             alt="Imagen"
                             loading="lazy"
                             @onclick="() => OpenPreview(image)"
                             style="
                            width: 100%;
                            height: 250px;
                            object-fit: cover;
                            display: block;" />
                    }
                    else if (image.ImageType == "video")
                    {
                        <div @onclick="() => OpenPreview(image)"
                             style="position: relative;">

                            <video style="
                                width: 100%;
                                height: 250px;
                                object-fit: cover;
                                display: block;
                                            ">
                                <source src="@image.Url" />
                            </video>

                            <div style="
                                position: absolute;
                                inset: 0;
                                display: flex;
                                align-items: center;
                                justify-content: center;
                                background: rgba(0,0,0,0.2);
                                            ">
                                <i class="bi bi-play-circle-fill"
                                   style="
                                    font-size: 60px;
                                    color: white;
                                                "></i>
                            </div>
                        </div>
                    }
                    else if (image.ImageType == "audio")
                    {
                        <div style="
                            height: 250px;
                            display: flex;
                            flex-direction: column;
                            align-items: center;
                            justify-content: center;
                            padding: 1rem;
                            background: #f8f9fa;
                                        ">

                            <i class="bi bi-music-note-beamed"
                               style="
                                font-size: 60px;
                                color: #3C6A79;
                                margin-bottom: 1rem;
                                            ">
                            </i>

                            <audio controls
                                   style="width: 100%;">
                                <source src="@image.Url" type="audio/mpeg" />
                            </audio>
                        </div>
                    }
                </div>
            }
        </div>
    }
</div>

<!-- PREVIEW -->
@if (PreviewImage != null && !string.IsNullOrEmpty(PreviewImage.Url))
{
    <div @onclick="ClosePreview"
         style="
            position: fixed;
            inset: 0;
            background-color: rgba(0,0,0,0.85);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 9999;
            padding: 1rem;
                 ">

        <div style="position: relative;" @onclick:stopPropagation>

            <!-- Descargar -->
            <button @onclick="() => DownloadImage(PreviewImage)"
                    style="
                        position: absolute;
                        top: 20px;
                        right: 80px;
                        background-color: #3C6A79;
                        color: white;
                        border: none;
                        border-radius: 50%;
                        width: 40px;
                        height: 40px;
                        cursor: pointer;
                        z-index: 10;
                            ">
                <i class="bi bi-download"></i>
            </button>

            <!-- Eliminar -->
            <button @onclick="() => ConfirmDelete(PreviewImage)"
                    style="
                        position: absolute;
                        top: 20px;
                        right: 20px;
                        background-color: #dc3545;
                        color: white;
                        border: none;
                        border-radius: 50%;
                        width: 40px;
                        height: 40px;
                        cursor: pointer;
                        z-index: 10;
                            ">
                <i class="bi bi-trash"></i>
            </button>

            @if (PreviewImage.ImageType == "imagen")
            {
                <img src="@PreviewImage.Url"
                     style="
                        max-width: 90vw;
                        max-height: 90vh;
                        border-radius: 10px;
                                " />
            }
            else if (PreviewImage.ImageType == "video")
            {
                <video controls autoplay
                       style="
                            max-width: 90vw;
                            max-height: 90vh;
                            border-radius: 10px;
                                    ">
                    <source src="@PreviewImage.Url" />
                </video>
            }
            else if (PreviewImage.ImageType == "audio")
            {
                <div style="
                        width: 400px;
                        max-width: 90vw;
                        background: white;
                        padding: 2rem;
                        border-radius: 12px;
                        text-align: center;
                                ">

                    <i class="bi bi-music-note-beamed"
                       style="
                            font-size: 80px;
                            color: #3C6A79;
                            margin-bottom: 1rem;
                            display: block;
                                    ">
                    </i>

                    <audio controls autoplay
                           style="width: 100%;">
                        <source src="@PreviewImage.Url" type="audio/mpeg" />
                    </audio>
                </div>
            }
        </div>
    </div>
}